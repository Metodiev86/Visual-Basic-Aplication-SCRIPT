IF  ([Вид на Операцията] == 'Кредитно известие') 
    THEN IF ([вид операция при Стокова] == 27)
        THEN
            [Обща Стойност]*[Платил/Неплатил]
        ELSE
            [Обща Стойност]*0
        END
    ELSE
    [Обща Стойност]*[Платил/Неплатил]*(-1)
END





2)

IF  ([Вид на Операцията] == 'Кредитно известие') 
    THEN IF ([вид операция при Стокова] == 27)
        THEN
            [Обща Стойност]*[Платил/Неплатил]
        ELSE
            [Обща Стойност]*0
        END
    ELSE if ([Вид на Операцията] == 'продажба') 
             THEN IF  ([вид операция при Стокова] == 27) 
               THEN
                [Обща Стойност]*0
              ELSE 
                [Обща Стойност]*[Платил/Неплатил]*(-1)
            END
            ELSEIF (([Вид на Операцията] == 'Дебитно известие') and ([вид операция при Стокова] == 27)) or (([Вид на Операцията] == 'Дебитно известие') and ([вид операция при Стокова] == 2))   
                THEN
                    [Обща Стойност]*0
            ELSE
                [Обща Стойност]*[Платил/Неплатил]*(-1)
            END
END